<div class="main">
  <div class="form-wrap">
    <div class="form__wrapper">
      <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
        <%= render 'shared/error_messages', model: f.object %>
        <div class="field">
          <label class="form-name">ユーザーアイコン</label>
          <span class="caution">※設定しない場合は、初期アイコンが設定されます</span>
          <div class="click-upload">
            <%= f.file_field :image, id:"item-image" %>
          </div>
        </div>
        <div class="field">
          <label class="form-name">ユーザー名</label>
          <%= f.text_field :name, class:"form-default", maxlength:"20", id:"user_name", autofocus: true %>
        </div>
        <div class="field">
          <label class="form-name">メールアドレス</label>
          <%= f.email_field :email, class:"form-default", id:"user_email", autocomplete: "email" %>
        </div>
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>
        <div class="field">
          <label class="form-name">新しいパスワード</label><br />
            <span class="caution">
              ※8文字以上の半角英数字を設定してください<br />
              パスワードを変更しない場合は、現在登録しているパスワードを入力してください
            </span>
          <%= f.password_field :password, class:"form-default", id:"user_password", autocomplete: "new-password" %>
        </div>
        <div class="field">
          <label class="form-name">新しいパスワード再入力</label><br />
            <span class="caution">
              パスワードを変更しない場合は、現在登録しているパスワードを入力してください
            </span>
          <%= f.password_field :password_confirmation, class:"form-default", id:"user_password_confirmation", autocomplete: "new-password" %>
        </div>
        <div class="field">
          <label class="form-name">現在登録しているパスワード</label>
          <%= f.password_field :current_password, class:"form-default", autocomplete: "current-password" %>
        </div>
        <div class="actions">
          <%= f.submit "更新する", class: :form__btn  %>
        </div>
      <% end %>
    </div>
  </div>
</div>